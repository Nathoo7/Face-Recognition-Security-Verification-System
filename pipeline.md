1. Run train.py to train "train_me" folder ("imperial_dataset" for Imperial dataset ) with training, validation and test set. create "checkpoints" folder with weight (best_model). 
2. Run Infer.py to extract the embeddings from the training set.
3. Run testing.py to compare training set with test set for evaluation. 
